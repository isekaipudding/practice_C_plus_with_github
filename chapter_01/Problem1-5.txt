#include <iostream>
#include <string>

using namespace std;

int main() {
    string name, age, address;

    cin >> name;
    cin >> age;
    cin >> address;

    cout << "name : " << name << "\n";
    cout << "age : " << age << "\n";
    cout << "address : " << address << "\n";

    return 0;
}