#include <iostream>

using namespace std;

int main() {
    int a, b, c;
    cout << "Input three Integers -> ";
    cin >> a >> b >> c;
    int min = a;
    min = (b<min) ? b : min;
    min = (c<min) ? c : min;

    cout << "Min number is " << min;

    return 0;
}