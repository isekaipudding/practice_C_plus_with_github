#include <iostream>

using namespace std;

int main() {
    double result = 0.0;

    for(int i = 1; i <= 100000; i++) {
        if(i & 1) {
            result += 4.0 / (2*i - 1);
        } else {
            result -= 4.0 / (2*i - 1);
        }
    }

    cout << result;

    return 0;
}