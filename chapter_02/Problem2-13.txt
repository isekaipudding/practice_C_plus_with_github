#include <iostream>

using namespace std;

int main() {
    int radius;
    cout << "Input sphere radius -> ";
    cin >> radius;
    cout << "Sphere Volume - > " << 4 * radius * radius * radius * 3.14 / 3;

    return 0;
}