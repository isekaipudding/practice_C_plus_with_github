#include <iostream>

using namespace std;

int main() {
    int a;
    cout << "Input the number : ";
    cin >> a;
    (a % 2 == 1) ? cout << a << " is odd number \n" : cout << a << " is even number \n";

    return 0;
}